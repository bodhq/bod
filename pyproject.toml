[project]
name = "bod"
version = "0.1.0"
description = "Monorepo pro modulární školní informační systém bod."
requires-python = ">=3.12"
dependencies = ["api"]

[tool.uv]
package = false

[tool.uv.sources]
api = { workspace = true }

[tool.uv.workspace]
members = ["apps/api"]
